name: vfocus
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  get: ^4.3.8
  
  hive: ^2.0.4
  hive_flutter: ^1.1.0

  http:
  
  cupertino_icons: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^1.0.0
  hive_generator: ^1.1.1
  build_runner:


flutter:
  uses-material-design: true
  #assets:
  #  - assets/
  #  - assets/images/
